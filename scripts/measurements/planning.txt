SimStep -> avg anger(spawned)

total anger when despawning -> #vehicles

SimStep -> # spawned vehicles

despawn_age -> #vehicles

despawn_age / route_distance -> #vehicles




Collecting:
1) simulation step (can be replaced by a simple loop)
2) anger per time step
3) # spawned vehicles per time step
4) total anger when despawning (order and vehicle ID doesn't matter)
5) age when despawning




PS:
- '# spawned vehicles' entspricht den Autos, die aktuell state == spawned haben. Das bedeutet, dass der Graph in zwei
Zeitschritten den selben Wert haben kann, obwohl Autos gespawned sind, weil genauso viele gedespawned sein können.
